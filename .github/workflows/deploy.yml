name: Legacy GitHub Pages deploy (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        run: |
          echo "GitHub Pages publishing is disabled for this repository."
          echo "The application has a server-side API and persisted contact/reservation data; publishing the repository as a static bundle is not a safe deployment path."
